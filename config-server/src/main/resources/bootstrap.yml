spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcellodesales/spring-cloud-config-demo-configs
          cloneOnStart: true
        bootstrap: true
      fail-fast: true